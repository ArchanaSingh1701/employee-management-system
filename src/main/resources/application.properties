spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/employee_management_system?useSSL=false&serverTimezone=Asia/Kolkata&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:ems_user}
spring.datasource.password=${DB_PASSWORD:EmsApp@2026}

# Validate the existing schema instead of changing it automatically on startup.
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:validate}
spring.jpa.show-sql=false
